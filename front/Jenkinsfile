pipeline {
  agent any
  environment {
    CI = 'true'
    HOME = '.'
  }
  stages {
    stage ('Install Dependecies') {
      steps {
        dir ("front/") {
          sh 'npm install'
        }
      }
    }
    stage ('Test') {
      steps {
        dir ("front/") {
          sh 'npm run test:unit'
        }
      }
    }
    stage ('Deploy') {
      steps {
        dir ("front/") {
          sh 'chmod +x ./scripts/deploy.sh'
          sh './scripts/deploy.sh'
        }
      }
    }
  }
}