pipeline {
  agent {
    docker {
      image 'node:10-alpine'
    }
  }
  stages {
    stage ('Build') {

      steps {
        dir ("front/") {
          sh 'strace -f -F -o whoami whoami'
          sh 'npm install'
        }
      }
    }
  }
}