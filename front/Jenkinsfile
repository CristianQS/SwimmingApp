pipeline {
  agent {
    docker {
      image 'node:10-alpine'
    }
  }
  stages {
    stage ('Build') {
      steps {
        sh 'cd ./front'
        sh 'ls'
        sh 'pwd'
        sh 'npm install'
      }
    }
  }
}